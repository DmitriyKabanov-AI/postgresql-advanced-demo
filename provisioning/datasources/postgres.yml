apiVersion: 1

datasources:
  - name: PostgreSQL DWH
    type: grafana-postgresql-datasource
    access: proxy
    url: db:5432
    database: demo
    user: postgres
    secureJsonData:
      password: postgres_pass
    jsonData:
      sslmode: disable
      postgresVersion: 1500
      timescaledb: false